# 请求配置（复制为 .env，改完需重启 pnpm dev）

# 开发时代理目标：不设或注释 = 走 mock；设了 = 连真实后端（需先 pnpm run dev:backend）
API_PROXY_TARGET=http://localhost:3000

# 生产/直连时 API 基础地址（可选，默认 /api）
# UMI_APP_API_BASE_URL=https://api.example.com
