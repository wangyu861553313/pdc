export const DEFAULT_NAME = 'Umi Max';

/** 与 app.tsx layout 共用：内容区内边距，与 ProLayout contentStyle.padding 一致 */
export const LAYOUT_CONTENT_PADDING = 16;

/** PageContentCard 默认配置，与 layout 布局统一 */
export const PAGE_CONTENT_CARD = {
  /** 内容区最大宽度（px） */
  // maxWidth: 1400,
  /** 头部默认背景色 */
  headerColor: '#cf1322',
} as const;
